service: github-3.12-sls-22feb-ong-joseph
frameworkVersion: 3

provider:
  name: aws
  runtime: nodejs18.x

function:
  api:
    handler: index.handler
    event:
      - httpApi:
          path: /
          method: get

plugins:
  - serverless-offline